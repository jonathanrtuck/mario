import { EntityConfiguration } from "@/types";

import { QuestionBlock } from "./QuestionBlock";

export const ENTITY_QUESTION_BLOCK: EntityConfiguration = {
  Component: QuestionBlock,
  type: "QuestionBlock",
};
